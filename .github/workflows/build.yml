name: Build & Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'

      - name: Print Supabase deps
        run: ./gradlew :app:dependencies --configuration debugCompileClasspath --no-daemon 2>&1 | grep -E "(supabase|gotrue|postgrest|kotlinx-datetime|auth)" | head -30

      - name: Build Debug APK
        run: ./gradlew assembleDebug --no-daemon -x lint

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: guide-trade-ai-debug-apk
          path: app/build/outputs/apk/debug/app-debug.apk

      - name: Run Unit Tests
        run: ./gradlew testDebugUnitTest --no-daemon
